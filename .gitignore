# deps
/node_modules

# Next.js
/.next/
/out/

# env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Python (scraper)
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# Motor de datos parqueado dentro del scraper: su .venv (105 MB) y su .git
# anidado NO deben entrar en este repo (no se despliega con la web).
/calendar_scraper/scrapers/.venv/
/calendar_scraper/scrapers/.git/

# Motor fantasy-atletismo-engine: repo propio (rrojjo/fantasy-atletismo-engine),
# clonado aquí en local y en CI para usar sus adapters (fuentes_engine.py).
# NO se commitea con la web.
/fantasy-atletismo-engine/

# log temporal del scraper
/scrape.log

# borradores de La Previa (scripts/generar_previa.py)
/previa_out/

# Workspace de scrapers de podios (repo aparte clonado en local; tiene su propio .git
# y commitearlo crearía un gitlink roto). Lo aprovechable ya está extraído en
# calendar_scraper/scraper-podios-extraido/.
/calendar_scraper/scrapers/
